$firefoxPath = "C:\Users\kushal\AppData\Local\Mozilla Firefox"
$env:Path = "$firefoxPath;$env:Path"

Start-Transcript -Path "C:\Users\kushal\src\myhtml\fromnepal\public\notes\output.txt"

# cd "C:\Users\kushal\src\angular\angulareighteen\"
# Get-Date
# Remove-Item -Recurse -Force .\node_modules
# Get-Date
# Remove-Item -Force .\yarn.lock
# Get-Date
# git fetch
# Get-Date
# git status
# Get-Date
# Get-Date
# git add .
# git commit --message "add all files" --message "update from terminal"
# Get-Date
# git pull --rebase origin master --strategy-option=theirs
# Get-Date
# git push origin master
# Get-Date
# yarn
# Get-Date
# yarn version --patch
# Get-Date
# yarn run ng update
# Get-Date
# yarn run ng build
# Get-Date
# Get-Date
# git fetch
# Get-Date
# git status
# Get-Date
# Get-Date
# git add .
# git commit --message "add all files" --message "update from terminal"
# Get-Date
# git pull --rebase origin master
# Get-Date
# git push origin master
# Get-Date
# yarn run ng test
# Get-Date
# yarn run ng update @angular/core @angular/cli @angular/material
# Get-Date
# git add .
# git commit --message "add all files" --message "update from terminal"
# Get-Date
# git pull --rebase origin master
# Get-Date
# git push origin master
# Get-Date
# echo y | npx update-browserslist-db@latest
# Get-Date
# git fetch
# Get-Date
# git status
# Get-Date
# Get-Date
# git add .
# git commit --message "add all files" --message "update from terminal"
# Get-Date
# git pull --rebase origin master
# Get-Date
# git push origin master
# Get-Date
# git remote show origin
# Get-Date

cd "C:\Users\kushal\RustroverProjects\helloworld\"
Get-Date
cargo clean
Get-Date
cargo build
Get-Date
cargo test --profile test --no-fail-fast
Get-Date
git add .
Get-Date
git status
Get-Date
git commit --message "build application" --message "from the terminal" --verbose
Get-Date
git pull --rebase origin master --verbose
Get-Date
git push origin master --verbose
Get-Date

C:\Users\kushal\src\myhtml\fromnepal\public\mydotnet.ps1

cd "C:\Users\kushal\src\myhtml\matchthree\matchthree\"
Get-Date
yarn
Get-Date
yarn run build
Get-Date
git add .
Get-Date
git status
Get-Date
git commit --message "build application" --message "from the terminal" --verbose
Get-Date
git pull --rebase origin master
Get-Date
git push origin master
Get-Date

cd "C:\Users\kushal\src\mylogging\"
Get-Date
git add .
Get-Date
git commit --message "add all files" --message "from the terminal"
Get-Date

$imageUrl = "https://cdn.star.nesdis.noaa.gov/GOES16/ABI/FD/GEOCOLOR/10848x10848.jpg"
$outputPath = "C:\Users\kushal\src\myhtml\fromnepal\public\notes\earth.jpeg"
Get-Date
Invoke-WebRequest -Uri $imageUrl -OutFile $outputPath
Get-Date
Write-Output "Image downloaded successfully to $outputPath"
Get-Date

cd "C:\Users\kushal\src\myhtml\fromnepal"
Get-Date
yarn
Get-Date
yarn run build
Get-Date

Stop-Transcript

git add .
Get-Date
git status
Get-Date
git commit --message "build application" --message "from the terminal" --verbose
Get-Date
git pull --rebase origin main
Get-Date
git push origin main
Get-Date
